<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xreadup.admin.mapper.SystemConfigMapper">

    <!-- 根据配置键查询配置 -->
    <select id="selectByConfigKey" resultType="com.xreadup.admin.model.entity.SystemConfig">
        SELECT * FROM system_config WHERE config_key = #{configKey}
    </select>

    <!-- 根据分类查询配置列表 -->
    <select id="selectByCategory" resultType="com.xreadup.admin.model.entity.SystemConfig">
        SELECT * FROM system_config WHERE category = #{category} ORDER BY config_key ASC
    </select>

    <!-- 根据配置键更新配置值 -->
    <update id="updateValueByKey">
        UPDATE system_config 
        SET config_value = #{configValue}, updated_at = NOW() 
        WHERE config_key = #{configKey}
    </update>

    <!-- 批量更新配置 -->
    <update id="batchUpdateConfigs">
        <foreach collection="configs" item="config" separator=";">
            UPDATE system_config 
            SET config_value = #{config.configValue}, updated_at = NOW() 
            WHERE config_key = #{config.configKey}
        </foreach>
    </update>

</mapper>
